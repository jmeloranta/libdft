- Include moving background contribution to chemical potential automatically in driver.c so that abs boundaries and imag time
  work properly with it.
- use the common propagate entry points in libgrid rather than calling aux routines directly.


